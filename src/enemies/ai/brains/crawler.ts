/**
 * Kriecher: walls and ceilings (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const crawlerBrain: EnemyBrain | null = null;
