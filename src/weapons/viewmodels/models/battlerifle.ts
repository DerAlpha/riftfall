/**
 * SR-17 „Hammerschlag“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildBattlerifle: ViewmodelBuilder | null = null;
