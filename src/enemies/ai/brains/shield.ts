/**
 * Schildträger: frontal shield, advance + bash (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const shieldBrain: EnemyBrain | null = null;
