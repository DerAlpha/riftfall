/**
 * FW-4 „Inferno“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildFlamethrower: ViewmodelBuilder | null = null;
