/**
 * Flieger: 3D flight without navmesh (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const flyerBrain: EnemyBrain | null = null;
