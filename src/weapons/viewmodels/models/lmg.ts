/**
 * LM-60 „Bollwerk“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildLmg: ViewmodelBuilder | null = null;
