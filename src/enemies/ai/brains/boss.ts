/**
 * Bosses: phase scripts (ai/bosses) (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const bossBrain: EnemyBrain | null = null;
