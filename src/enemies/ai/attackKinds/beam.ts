/**
 * 'beam' attack executor (M6). null = not built yet: the strike is void (enemy recovers).
 */
import type { AttackExecutor } from './types';

export const beamAttack: AttackExecutor | null = null;
