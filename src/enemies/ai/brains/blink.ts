/**
 * Leerenspringer: teleports between cover (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const blinkBrain: EnemyBrain | null = null;
