/**
 * AS-20 „Mahlstrom“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildAutoshotgun: ViewmodelBuilder | null = null;
