/**
 * Glutkoloss (Boss) – enemy visual def (M6). null = not built yet. Import runtime values only from
 * '../enemyVisualCommon' and TYPES from '../enemyVisuals' (runtime cycle otherwise).
 */
import type { EnemyVisualDef } from '../enemyVisuals';

export const EMBERKOLOSS_VISUAL: EnemyVisualDef | null = null;
