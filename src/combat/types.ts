/**
 * Combat-module extensions of the CombatWorldApi contract used by the weapon system
 * (penetration-aware raycasts, prop impulses). They live in core/contracts.ts; re-exported here
 * for existing imports.
 */
export type { CombatRaycastOptions, WeaponCombatApi } from '../core/contracts';
