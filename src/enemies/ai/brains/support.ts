/**
 * Heiler/Beschwörer: keep distance, heal / summon (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const supportBrain: EnemyBrain | null = null;
