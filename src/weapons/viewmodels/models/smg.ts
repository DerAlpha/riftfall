/**
 * SK-5 „Viper“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildSmg: ViewmodelBuilder | null = null;
