/**
 * Milbe – enemy type def (M6). null = not built yet (the type cannot spawn). Import runtime values
 * only from '../enemyCommon' and TYPES from '../enemies' (runtime cycle otherwise).
 */
import type { EnemyTypeDef } from '../enemies';

export const MITE_ENEMY: EnemyTypeDef | null = null;
