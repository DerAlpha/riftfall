/**
 * KV-9 „Kolibri“ – procedural viewmodel (M5). null = not built yet: the rig shows the placeholder device.
 */
import type { ViewmodelBuilder } from '../index';

export const buildVector: ViewmodelBuilder | null = null;
