/**
 * Späher: long-range telegraphed laser (M6 brain). null = not built yet: types using it cannot spawn (logged once).
 */
import type { EnemyBrain } from '../types';

export const sniperBrain: EnemyBrain | null = null;
